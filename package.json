{
  "name": "magic-metal-sales-funnel",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "serve": "serve . -l 4173 --no-clipboard",
    "validate:html": "html-validate index.html privacy.html",
    "test:e2e": "playwright test",
    "test:e2e:ui": "playwright test --ui",
    "lighthouse": "lhci autorun",
    "quality": "npm run validate:html && npm run test:e2e && npm run lighthouse"
  },
  "devDependencies": {
    "@lhci/cli": "^0.15.1",
    "@playwright/test": "^1.60.0",
    "html-validate": "^10.1.2",
    "serve": "^14.2.5"
  },
  "engines": {
    "node": ">=20"
  }
}